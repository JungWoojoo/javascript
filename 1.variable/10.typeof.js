// typeof : 데이터 타입을 확인
// 값을 타입 문자열로 반환
let variable;
console.log(typeof variable);

variable = '';
console.log(typeof variable);

variable = 123;
console.log(typeof variable);

// 자바스크립트에도 타입이 동적으로 결정됨
// weakly -> 한번 결정되면 그 타입만 되는 것이 아니라 어떤 값이냐에 따라 타입 달라짐